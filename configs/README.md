# Configs

This folder holds versioned configuration files for:
- dataset ingestion/preprocessing
- split generation
- training (JEPA encoder, transition predictor)
- evaluation + reporting

Prefer config-driven runs over ad-hoc flags to keep experiments reproducible.

